;;; init-dockerfile-mode --- Summary
;;; Commentary:
;;; Code:
(require 'dockerfile-mode)
(add-to-list 'auto-mode-alist '("Dockerfile\\'" . dockerfile-mode))
(provide 'init-dockerfile-mode)
;;; init-dockerfile-mode ends here
